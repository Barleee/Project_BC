Design of a taxi fleet and driver management system -
System login
- view, add, delete, and update the list of taxis (must include coordinates) + map with points of interest
- view, add, delete, and update the list of customers for all taxis (must include coordinates) + map with locations 
- viewing, adding, deleting, and updating the list of drivers (must include coordinates) + map with locations
- viewing the list of customers for a selected taxi on a selected day
